sap.ui.define(['sap/fe/core/AppComponent'], function (AppComponent) {
  'use strict';

  return AppComponent.extend('purchase.orders.Component', {
    metadata: {
      manifest: 'json'
    }
  });
});