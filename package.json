{
  "name": "po-approval",
  "version": "1.0.0",
  "description": "Purchase Order approval automation on SAP BTP - CAP service, Fiori Elements UI, SAP Integration Suite (CPI) integration.",
  "license": "UNLICENSED",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@cap-js/sqlite": "^3",
    "@sap/cds": "^10",
    "@sap/xssec": "^4.15.0"
  },
  "devDependencies": {
    "@cap-js/cds-test": "^1.0.2",
    "@eslint/js": "^9",
    "eslint": "^9",
    "puppeteer-core": "^25.10.0"
  },
  "scripts": {
    "start": "cds-serve",
    "watch": "cds watch",
    "build": "cds build --production",
    "test": "node --test",
    "lint": "eslint srv test",
    "deploy:cf": "cf deploy mta_archives/po-approval_1.0.0.mtar"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite"
      },
      "auth": {
        "kind": "mocked"
      }
    }
  }
}
